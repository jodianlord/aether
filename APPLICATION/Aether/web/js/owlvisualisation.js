$('.owl-carousel').owlCarousel({
    items: 4,
    loop: false,
    center: true,
    autoWidth: true,
    margin: 10,
    URLhashListener: true,
    autoplayHoverPause: true,
    startPosition: 'URLHash'
});